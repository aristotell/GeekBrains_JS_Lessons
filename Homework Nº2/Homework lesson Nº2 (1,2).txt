1. ƒан код:
	var a = 1, b = 1, c, d;
	c = ++a; alert(c); // 2 
	d = b++; alert(d); // 1 ()
	c = (2+ ++a); alert(c); // 5 () 
	d = (2+ b++); alert(d); // 4 ()
	alert(a); // 3 (‘инальное значение переменной в даний момент)
	alert(b); // 3 (‘инальное значение переменной в даний момент)
ѕочему код даЄт именно такие результаты?

a. ќпераци€ префикс-инкремента (++a) возвращает значение уже измененной переменной "a" в переменной "с".
b. ќпераци€ пост-инкремента (b++) возвращает значение переменной "d" до выполнени€ инкремента "b".
c.   "2" добавл€ем уже увеличеное на единицу значение "а" которое с 2 изменилось на 3.
d.   "2" добавл€ем актуальное значение "b" а потом увеличеваем его на единицу.
e. (‘инальные значени€ переменних в даний момент)

2. „ему будет равен x в примере ниже?
	var a = 2;
	var x = 1 + (a *= 2);

«начение х равен 5.
ј происходит следущее: 1 + (2 * 2)